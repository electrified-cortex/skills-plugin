.: Content-hash-keyed durable record store. Probe / Read / Write / Invalidate API. Replaces .audit-reports/ and .code-reviews/ with one substrate that consumer skills call into directly, hash record, hash record skill, hash record workflow
hash-record-check: Probe the hash-record cache for a file. Triggers — cache check hash-record, check hash-record hit or miss, hash-record cache probe, skip if cached, cache-first lookup, check hash cache before running operation, hash record check, hash record check skill, hash record check workflow
hash-record-index: Build and refresh manifest.yaml index files inside each hash directory under .hash-record/. Triggers — index hash records, refresh hash-record manifest, build manifest.yaml, accelerate prune, hash record index, hash record index skill, hash record index workflow
hash-record-manifest: Compute a manifest hash for a set of files. Triggers — compute manifest hash, multi-file cache key, hash-record manifest, manifest hash, bundle file hashes, cache key for directory, hash record manifest, hash record manifest skill, hash record manifest workflow
hash-record-prune: Remove orphaned hash directories from a repository's .hash-record/ store. Triggers — prune hash records, clean up hash-record, remove orphaned records, hash-record maintenance, reclaim disk, hash record prune, hash record prune skill, hash record prune workflow
