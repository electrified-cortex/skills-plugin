.: Decision tree for whether, how, and at what model tier to dispatch sub-agents, dispatch, dispatch skill, dispatch workflow
dispatch-setup: Configure the dispatch skill agent file correctly in VS Code and Cursor. Triggers — dispatch setup, configure, dispatch setup, dispatch setup skill, dispatch setup workflow