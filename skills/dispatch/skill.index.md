# dispatch - skill index

## dispatch

Supports Decision tree for whether, how, and at what model tier to dispatch sub-agents.

## setup

Supports Configure the dispatch skill agent file correctly in VS Code and Cursor Triggers — dispatch setup, configure.
